# Copie este arquivo para .streamlit/secrets.toml (que está no .gitignore)
# e cole sua chave real.
#
# Para deploy no Streamlit Cloud, configure o mesmo segredo em:
# Settings -> Secrets, no formato TOML idêntico a este.

GEMINI_API_KEY = "cole_sua_chave_aqui"
